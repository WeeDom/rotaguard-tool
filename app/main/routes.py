from flask import Blueprint, render_template, request, redirect, url_for, make_response

main_bp = Blueprint("main", __name__)

"""
This blueprint is now empty. All web routes have been removed.
Flask only serves API endpoints; frontend is handled by React/Vite.
"""
